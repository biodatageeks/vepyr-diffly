FROM python:3.11-slim

WORKDIR /workspace

RUN apt-get update \
    && apt-get install -y --no-install-recommends build-essential cargo \
    && rm -rf /var/lib/apt/lists/*

COPY docker/vepyr_runner.py /opt/vepyr_diffly/vepyr_runner.py

ENTRYPOINT ["python", "/opt/vepyr_diffly/vepyr_runner.py"]
