[PLANS]
- 2026-03-27T21:40:00+01:00 [USER] Create a new repository to compare Ensembl VEP results against biodatageeks/vepyr using Python DataFrame-based diffs, with container-first execution and staged support for cache/flag combinations.
- 2026-03-30T07:41:04Z [USER] Rework compare into a RAM-safe bounded-memory algorithm that can process very large annotated VCFs without relying on full in-memory materialization.

[DECISIONS]
- 2026-03-27T21:40:00+01:00 [USER] Initialize a new git repository, but perform no other git mutations.
- 2026-03-27T21:40:00+01:00 [USER] v1 scope is run-and-compare, not compare-only.
- 2026-03-27T21:40:00+01:00 [USER] Canonical comparison is two-tier: variant/allele level and consequence-level drill-down.
- 2026-03-27T21:40:00+01:00 [USER] Default execution mode is container-first.
- 2026-03-27T21:40:00+01:00 [USER] v1 preset support starts with `--everything` plus Ensembl cache; architecture must stage future `merged` and `refseq`.
- 2026-03-27T21:40:00+01:00 [USER] Canonical equality is parsed semantic CSQ comparison rather than raw-string equality.
- 2026-03-27T21:40:00+01:00 [USER] Default `vepyr` source is a local checkout path.

[PROGRESS]
- 2026-03-27T21:40:00+01:00 [TOOL] Verified the workspace started empty and initialized git in `/Users/lukaszjezapkowicz/Desktop/magisterka/praca/vepyr_diffly`.
- 2026-03-27T22:05:00+01:00 [CODE] Added initial repository scaffold: project metadata, local `AGENTS.md`, staged preset manifest, container definitions, Python package, fixture VCFs, and unit/integration tests.
- 2026-03-27T22:05:00+01:00 [CODE] Implemented CLI commands `run`, `list-presets`, and `inspect-run`, plus semantic VCF normalization, tiered comparison, reporting, and runtime artifact handling.
- 2026-03-27T22:20:00+01:00 [CODE] Added root `requirements.txt`, documented local environment setup in `README.md`, created `.venv`, and expanded `.gitignore` to cover virtualenv, coverage, wheel metadata, IDE files, and local override artifacts.
- 2026-03-27T22:50:00+01:00 [CODE] Added repo-local `.env` configuration support with `.env.example`, automatic CLI loading, and README documentation for path-based runtime settings.
- 2026-03-27T23:00:00+01:00 [CODE] Cloned `biodatageeks/vepyr` into repo root under `vepyr/`, added it to `.gitignore`, and pointed local `.env` plus `.env.example` at that checkout path.
- 2026-03-27T22:49:52+0100 [CODE] Fixed local `vepyr` execution by preserving the configured `.../.vepyr/bin/python` symlink path instead of resolving it to the system interpreter, then aligned normalized count columns to `Int64` for `dataframely` schema validation.
- 2026-03-27T22:49:52+0100 [CODE] Added regression tests for preserved `vepyr_python` interpreter paths and normalized integer count dtypes; updated `README.md` with the verified local execution workflow and smoke-run result.
- 2026-03-27T23:01:37+0100 [CODE] Expanded `README.md` into an operator-facing runbook with `How To Run`, `Expected Output`, `Troubleshooting`, and `Known Limitations` sections that distinguish verified local behavior from staged future work.
- 2026-03-28T08:54:50+0100 [TOOL] Executed a full unsampled golden run into `runs/full-golden-20260327`; both annotation engines finished and wrote full annotated VCFs plus runtime logs, but the Python comparison layer did not emit summary or diff artifacts.
- 2026-03-28T08:54:50+0100 [CODE] Added `README.md` and `runs/full-golden-20260327/RUN_STATUS.md` notes describing the observed full golden run outcome and its current scale limitation.
- 2026-03-28T09:23:46+0100 [CODE] Added `compare-existing` CLI support so the repo can compare already annotated VCF outputs without rerunning VEP or `vepyr`.
- 2026-03-28T09:23:46+0100 [CODE] Refactored normalization to materialize variant and consequence summaries into parquet artifacts, and refactored comparison to run `diffly` on `LazyFrame` parquet scans with timestamped progress logging.
- 2026-03-28T09:23:46+0100 [CODE] Updated `README.md` with compare-only usage, progress-log semantics, and the new reuse-of-annotated-VCFs workflow.
- 2026-03-28T11:33:00+01:00 [CODE] Replaced the old full consequence merge path with a bucketized pipeline: chunked VCF parsing, Polars-based consequence normalization into hash buckets, and concurrent `diffly` execution per bucket with merged final diff artifacts.
- 2026-03-28T11:33:00+01:00 [CODE] Added adaptive bucket counts for smaller runs, directory-aware heartbeat logging, and a `ThreadPoolExecutor` fallback when process pools are unavailable in constrained environments.
- 2026-03-28T11:33:00+01:00 [CODE] Updated `README.md` to describe bucketized consequence comparison, per-bucket progress milestones, and the new normalized bucket artifact directories.
- 2026-03-28T12:40:00+01:00 [CODE] Tightened the bucketized compare path further: left/right variant summaries now materialize in parallel, left/right consequence bucketization runs in parallel subprocesses, bucket compare uses dedicated subprocess workers, and mismatch TSV output is capped to a sampled prefix to reduce unnecessary full-run I/O.
- 2026-03-28T12:40:00+01:00 [CODE] Updated `README.md` with current performance notes and the fact that mismatch TSV artifacts are intentionally sampled while parquet diff artifacts remain complete.
- 2026-03-28T13:44:00+01:00 [CODE] Changed `run` input preparation to match the older `annotator_testing` semantics for `vepyr`: sample first when requested, then decompose multi-allelic VCF rows into single-alt rows before invoking both annotators, and persist preparation stats in `runtime/input_preparation.json`.
- 2026-03-28T13:44:00+01:00 [CODE] Added unit coverage for multi-allelic input decomposition and documented the prepared-input artifacts in `README.md`.
- 2026-03-28T14:54:00+01:00 [CODE] Improved consequence compare progress logging for future runs: shard workers now log per-bucket completion immediately as `worker: completed bucket XXXX (n/N in shard)`, while the parent process continues to log global `consequence: completed bucket XXXX (n/total)` milestones.
- 2026-03-28T16:20:00+01:00 [CODE] Reworked `README.md` so it starts with a local operator quickstart, then the comparison model, then a results chapter split into `A. Smoke Tests` and `B. Golden Test`.
- 2026-03-28T16:35:00+01:00 [CODE] Hardened `run` against stale runtime artifacts by deleting previous `runtime/prepared_input.vcf`, `runtime/vep.annotated.vcf`, and `runtime/vepyr.annotated.vcf` before starting a new local annotation into an existing run directory.
- 2026-03-28T16:40:00+01:00 [CODE] Expanded `README.md` with a short technical summary of the compare architecture: canonical input preparation, annotated VCF normalization, `diffly`-based tiered comparison, and bucketized full-scale consequence diffs.
- 2026-03-28T16:42:00+01:00 [CODE] Added an ASCII pipeline diagram to `README.md` covering input preparation, VEP/`vepyr` annotation, normalization, `diffly` comparison, and output artifacts.
- 2026-03-29T03:25:00+01:00 [CODE] Extended run-directory cleanup so rerunning `vepyr-diff run` in the same `--output-dir` deletes stale summaries, diff artifacts, progress logs, sampled/prepared inputs, annotated VCFs, and normalized parquet directories before starting a fresh run.
- 2026-03-30T07:41:04Z [CODE] Added a bounded-memory compare planner with public `--memory-budget-mb` / `VEPYR_DIFFLY_MEMORY_BUDGET_MB` support, summary/report propagation of the effective resource plan, and new variant bucket artifact directories in runtime cleanup and run metadata.
- 2026-03-30T07:41:04Z [CODE] Reworked the large-file compare path so variant normalization can stream VCF records into per-bucket spill files, compact those buckets to deterministic `bucket.parquet` artifacts, and compare the variant tier bucket-by-bucket instead of running one global `diffly` over the full variant table.
- 2026-03-30T07:41:04Z [CODE] Removed the previous default left/right bucketization subprocess fanout for heavy compare-only runs and replaced it with planner-controlled sequencing or limited parallelism in-process, so side concurrency is now budget-aware instead of always-on.
- 2026-03-30T10:22:00+02:00 [CODE] Fixed local `vepyr` execution again by changing `vepyr_runner.py` to prefer importing `vepyr` from the active `.vepyr` interpreter environment first and only fall back to the repo checkout path if that import fails; this avoids shadowing a working compiled install with the unbuilt `vepyr/src` checkout.
- 2026-03-30T11:15:00+02:00 [CODE] Updated `README.md` to match the current bounded-memory compare implementation, removed stale historical guidance, and documented the retained `runs/full-golden-compare-fast` result with stage timings, mismatch summary, and current hypotheses about consequence-level drift.

[DISCOVERIES]
- 2026-03-27T21:40:00+01:00 [TOOL] Existing `annotator_testing` runner compares VCF `INFO.CSQ` semantically and already contains `vepyr` adapter logic worth mirroring conceptually.
- 2026-03-27T21:40:00+01:00 [TOOL] `diffly` requires Python >=3.11 and compares Polars DataFrames using unique primary keys.
- 2026-03-27T21:40:00+01:00 [TOOL] `polars-bio` exposes `scan_vcf` / `read_vcf` and supports lazy VCF ingestion.
- 2026-03-27T21:40:00+01:00 [TOOL] Golden input exists at `~/Downloads/HG002_GRCh38_1_22_v4.2.1_benchmark.vcf` and is about 2.6G.
- 2026-03-27T22:05:00+01:00 [TOOL] The local host Python lacked `pytest`, `polars`, `rich`, `diffly`, `dataframely`, and `polars_bio`, so runtime validation was limited to static compilation and CLI smoke tests that avoid those imports.
- 2026-03-27T22:35:00+01:00 [TOOL] `polars-bio` exposes VCF positions as `start`/`end` rather than `pos`; the adapter now maps `start + 1` back to VCF-style 1-based `POS`.
- 2026-03-27T22:35:00+01:00 [TOOL] Minimal fixture VCFs can fail in `polars-bio` / DataFusion with `VCF read error: unexpected EOL`, so normalization now falls back to a simple text parser for those cases.
- 2026-03-27T22:35:00+01:00 [TOOL] Docker is available locally, but no local `vepyr` checkout was found under `/Users/lukaszjezapkowicz/Desktop/magisterka/praca`, and no concrete VEP cache path has been resolved yet.
- 2026-03-27T22:50:00+01:00 [TOOL] `terraform.tfvars` in `annotator_testing` exposes `vep_cache_dir=/Users/lukaszjezapkowicz/.vep`, the GRCh38 FASTA under that cache, and the local `vep_bin` path; these values were mirrored into `.env.example` and local `.env`.
- 2026-03-27T23:00:00+01:00 [TOOL] Root-local `vepyr/` checkout now exists and contains `pyproject.toml`; `git status` confirms the directory is ignored.
- 2026-03-27T22:49:52+0100 [TOOL] Resolving the configured `VEPYR_DIFFLY_VEPYR_PYTHON` symlink path breaks the `vepyr` runtime on macOS because `/.../.vepyr/bin/python` resolves to `/Library/Frameworks/.../python3.12`, which loses the virtualenv site-packages and caused `ModuleNotFoundError: No module named 'vepyr'`.
- 2026-03-27T22:49:52+0100 [TOOL] Real smoke-run normalization produced unsigned aggregate counts from Polars (`UInt32`), while the contract schema expected signed `Int64`; explicit casting in normalization resolves that mismatch cleanly.
- 2026-03-27T22:49:52+0100 [TOOL] Existing external assets are sufficient for local end-to-end execution: VEP binary and cache under `/Users/lukaszjezapkowicz/.vep`, `vepyr` interpreter under `annotator_testing/runner/.vepyr/bin/python`, and parquet cache root under `annotator_testing/.cache/cache_testing/vepyr_cache`.
- 2026-03-28T08:54:50+0100 [TOOL] Full golden annotation completed successfully for both engines with identical annotated record counts (`4048342` VCF records each), producing `runtime/vep.annotated.vcf` at about `16G` and `runtime/vepyr.annotated.vcf` at about `15G`.
- 2026-03-28T08:54:50+0100 [TOOL] The unsampled full golden `run` did not produce `summary.json` or diff artifacts; the visible terminal ending was a Python `multiprocessing.resource_tracker` warning about one leaked semaphore, which points to the Python-side normalization/comparison stage rather than the engine runtimes.
- 2026-03-28T09:23:46+0100 [TOOL] `polars-bio` may expose `CSQ` as a `List[String]` column rather than a scalar string; joining list values before downstream normalization avoids eager fallback and enables lazy materialization on real annotated outputs.
- 2026-03-28T09:23:46+0100 [TOOL] Stale idle `compare-existing` processes can linger at `0.0% CPU` and block fresh attempts; terminating those stale processes restored normal compare-only execution.
- 2026-03-28T09:23:46+0100 [TOOL] Compare-only on smoke annotated outputs now succeeds via the new path and writes `runtime/compare.progress.log`, normalized parquet summaries, diff artifacts, and run summaries.
- 2026-03-28T11:33:00+01:00 [TOOL] The earlier `100%` progress confusion came from finishing chunk scan while still needing a global merge; bucketized compare removes that hidden second full-pass stage and exposes bucket completion directly.
- 2026-03-28T11:33:00+01:00 [TOOL] `ProcessPoolExecutor` can fail in this macOS sandbox with `PermissionError: [Errno 1] Operation not permitted` during semaphore checks; `ThreadPoolExecutor` works as a safe fallback and still benefits native Polars/diffly work.
- 2026-03-28T11:33:00+01:00 [TOOL] Fixed bucket counts are wasteful on small inputs; an adaptive policy (`8` for local smoke artifacts) materially reduces overhead without changing semantics.
- 2026-03-28T12:40:00+01:00 [TOOL] Writing full mismatch TSVs on large runs adds avoidable I/O pressure after the heavy compare work is already done; keeping parquet as the canonical full artifact and sampling TSV output preserves usability while cutting that tail cost.
- 2026-03-28T12:40:00+01:00 [TOOL] The bucketized smoke compare now launches multiple subprocess compare workers successfully in this environment and completes with exact agreement, so the subprocess-worker path is verified rather than only assumed.
- 2026-03-28T13:30:00+01:00 [TOOL] The `5000`-record smoke mismatch pattern was concentrated on multi-allelic indels and disappeared after splitting sampled multi-allelic rows into single-alt rows before annotation; this matches the existing note in `annotator_testing` that meaningful V4/`vepyr` comparison should use normalized single-alt input semantics.
- 2026-03-28T13:30:00+01:00 [TOOL] The earlier `5000` consequence mismatches were not variant-level disagreements: the variant tier stayed exactly equal while the consequence tier alone diverged, which isolated the problem to multi-allelic `CSQ` semantics rather than basic engine disagreement on variant identity.
- 2026-03-28T14:54:00+01:00 [TOOL] The missing visible bucket progress during full consequence compare came from worker processes only reporting after a whole shard finished; logging after each bucket inside the worker restores near-real-time bucket progress in the shared `compare.progress.log`.
- 2026-03-28T15:20:00+01:00 [TOOL] The completed full compare in `runs/full-golden-compare-current` is not yet interpretable as a clean biological VEP-vs-`vepyr` consequence diff. Sampled mismatch rows (`2000/2000`) all came from multi-allelic loci in the older annotated VCFs, and raw `vepyr` `CSQ` entries at those loci contain `81` pipe-delimited fields while the shared `CSQ` header declares `80`. Example at `chr2:72305322`: VEP `CSQ` prefix `TGTATA|intron_variant|MODIFIER|...` vs `vepyr` `TGTATA|GTGTATATGTATA|intron_variant|MODIFIER|...`, which shifts downstream field interpretation.

[OUTCOMES]
- 2026-03-27T21:40:00+01:00 [ASSUMPTION] UNCONFIRMED until verification: the first implementation will favor deterministic local unit tests and provide runtime containers/configuration without executing real VEP inside this session.
- 2026-03-27T22:05:00+01:00 [TOOL] Verified `PYTHONPATH=src python3 -m compileall src tests`, `PYTHONPATH=src python3 -m vepyr_diffly.cli list-presets`, and `PYTHONPATH=src python3 -m vepyr_diffly.cli inspect-run --run-dir /tmp/vepyr-diffly-smoke`.
- 2026-03-27T22:05:00+01:00 [TOOL] UNCONFIRMED: `pytest` suite and end-to-end containerized `run` command were not executed in this session because required Python/runtime dependencies are not installed locally.
- 2026-03-27T22:20:00+01:00 [TOOL] Installed local Python dependencies into `.venv` from `requirements.txt` and verified `.venv/bin/python -m vepyr_diffly.cli list-presets`.
- 2026-03-27T22:35:00+01:00 [TOOL] Verified `.venv/bin/pytest -q` with `7 passed`.
- 2026-03-27T22:35:00+01:00 [TOOL] UNCONFIRMED: full `vepyr-diff run ...` against real VEP remains blocked on user-provided or discoverable `vepyr` checkout path and VEP cache location.
- 2026-03-27T22:50:00+01:00 [TOOL] Verified `.env` loading via `vepyr_diffly.settings.load_repo_env()`, `.venv/bin/python -m vepyr_diffly.cli list-presets`, and `.venv/bin/pytest -q` after adding `python-dotenv`.
- 2026-03-27T23:00:00+01:00 [TOOL] Verified local `.env` resolves `VEPYR_DIFFLY_VEPYR_PATH=/Users/lukaszjezapkowicz/Desktop/magisterka/praca/vepyr_diffly/vepyr` and `PYTHONPATH=src .venv/bin/python -m vepyr_diffly.cli list-presets` still works.
- 2026-03-27T22:49:52+0100 [TOOL] Verified `PYTHONPATH=src .venv/bin/pytest -q` with `8 passed` after adding regression coverage for the local runtime fixes.
- 2026-03-27T22:49:52+0100 [TOOL] Verified end-to-end local smoke run with `PYTHONPATH=src .venv/bin/python -m vepyr_diffly.cli run --output-dir runs/local-smoke-fix2`: variant tier `1000` joined equal, consequence tier `34741` joined equal, and zero mismatches in both tiers.
- 2026-03-27T22:49:52+0100 [TOOL] Verified `PYTHONPATH=src .venv/bin/python -m vepyr_diffly.cli inspect-run --run-dir runs/local-smoke-fix2` and confirmed `summary.json`, parquet diffs, TSVs, and runtime logs were written to the run directory.
- 2026-03-27T23:01:37+0100 [CODE] `README.md` now documents the exact local setup, smoke-run commands, output expectations, troubleshooting flow, and current verified limitations without changing runtime behavior.
- 2026-03-28T08:54:50+0100 [TOOL] Verified the full golden runtime artifacts exist in `runs/full-golden-20260327/runtime`, including complete `vep` and `vepyr` annotated VCFs and logs; final comparison summary artifacts are absent.
- 2026-03-28T08:54:50+0100 [ASSUMPTION] UNCONFIRMED until a scaling refactor is implemented: current full-frame normalization and comparison are the likely reason the unsampled full golden run does not complete on this machine.
- 2026-03-28T09:23:46+0100 [TOOL] Verified `PYTHONPATH=src .venv/bin/python -m pytest -q` with `9 passed` after adding compare-only and lazy parquet normalization changes.
- 2026-03-28T09:23:46+0100 [TOOL] Verified `compare-existing` against smoke artifacts in `runs/compare-only-smoke-clean` with variant tier `1000` joined equal and consequence tier `34741` joined equal, plus `runtime/compare.progress.log`.
- 2026-03-28T09:23:46+0100 [TOOL] A fresh full compare-only attempt is running in `runs/full-golden-compare-20260328c`; it has already materialized `normalized/left.variant.parquet` with `4048342` rows and advanced to left consequence summary materialization.
- 2026-03-28T11:33:00+01:00 [TOOL] Verified `PYTHONPATH=src .venv/bin/pytest -q` with `11 passed` after the bucketized consequence refactor.
- 2026-03-28T11:33:00+01:00 [TOOL] Verified bucketized compare-only smoke run with `PYTHONPATH=src .venv/bin/python -m vepyr_diffly.cli compare-existing --preset ensembl_everything --left-vcf runs/local-smoke-fix2/runtime/vep.annotated.vcf --right-vcf runs/local-smoke-fix2/runtime/vepyr.annotated.vcf --output-dir runs/compare-only-bucketed-smoke-v2`: variant tier `1000` joined equal, consequence tier `34741` joined equal, zero mismatches, and explicit per-bucket progress in `runtime/compare.progress.log`.
- 2026-03-28T11:33:00+01:00 [ASSUMPTION] UNCONFIRMED until rerun: the refactored bucketized compare path should scale materially better on the full golden annotated VCFs than the older global consequence merge path, but the full unsampled compare has not yet been re-executed after this refactor.
- 2026-03-28T12:40:19+01:00 [TOOL] Verified the newer subprocess-worker bucketized smoke path with `PYTHONPATH=src .venv/bin/python -m vepyr_diffly.cli compare-existing --preset ensembl_everything --left-vcf runs/local-smoke-fix2/runtime/vep.annotated.vcf --right-vcf runs/local-smoke-fix2/runtime/vepyr.annotated.vcf --output-dir runs/compare-only-bucketed-smoke-v5`: `13 passed` in the test suite first, then variant tier `1000` joined equal and consequence tier `34741` joined equal with zero mismatches.
- 2026-03-28T13:44:30+01:00 [TOOL] Verified `PYTHONPATH=src .venv/bin/pytest -q` with `15 passed` after adding canonical prepared-input splitting tests.
- 2026-03-28T13:44:30+01:00 [TOOL] Verified `PYTHONPATH=src .venv/bin/python -m vepyr_diffly.cli run --output-dir runs/local-smoke-5000-fixed --sample-first-n 5000` after multi-allelic input decomposition: `runtime/input_preparation.json` recorded `5000` sampled records expanded to `5021` prepared rows from `21` split source records, and the final comparison reported variant tier `5021` joined equal and consequence tier `93694` joined equal with zero mismatches.
- 2026-03-28T14:54:39+01:00 [TOOL] Verified the new per-bucket progress logging on `runs/compare-only-bucketed-smoke-progress`: `compare.progress.log` now contains both `worker: completed bucket XXXX (n/N in shard)` lines and global `consequence: completed bucket XXXX (n/total)` lines, while the smoke result remained fully equal.
- 2026-03-28T15:20:00+01:00 [TOOL] Verified the completed compare-only golden run in `runs/full-golden-compare-current`: variant tier was fully equal (`4048342` joined equal, `0` left-only/right-only/unequal), while consequence tier reported `871239` left-only and `172810` right-only rows with `0` unequal rows. Follow-up analysis showed the sampled mismatch artifacts are dominated by malformed old multi-allelic `vepyr` `CSQ` rows rather than confirmed semantic disagreements in shared normalized consequence rows.
- 2026-03-28T16:20:00+01:00 [TOOL] Verified a fresh end-to-end smoke matrix for sample sizes `10, 100, 1000, 5000, 10000, 25000, 50000, 100000` under `runs/smoke-matrix-full`. Every run was fully equal on both variant and consequence tiers after canonical single-alt input preparation. The generated matrix was saved in `runs/smoke-matrix-full/results.tsv` and summarized in `README.md`.
- 2026-03-28T16:20:00+01:00 [TOOL] Verified `PYTHONPATH=src .venv/bin/pytest -q` with `15 passed` after documenting the smoke matrix in `README.md`.
- 2026-03-28T16:35:00+01:00 [TOOL] Verified `PYTHONPATH=src .venv/bin/pytest -q tests/test_runtime.py` with `2 passed` after adding regression coverage for stale runtime output cleanup.
- 2026-03-29T03:19:00+01:00 [TOOL] A fresh full `run` into `runs/full-golden-fresh` failed inside `vepyr.annotate(...)` after processing about `1.25M / 4.10M` prepared variants. `runtime/vepyr.log` ended with `RuntimeError: VCF annotation failed: Parquet error: External: Generic LocalFileSystem error: Out of range of file .../vepyr_cache/parquet/115_GRCh38_vep/transcript/chr5.parquet, expected: 8, actual: 0`, which points at the external `vepyr` parquet cache rather than the local compare pipeline.
- 2026-03-29T03:25:00+01:00 [TOOL] Verified `PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_runtime.py` with `2 passed` after extending run-directory cleanup.
- 2026-03-29T10:45:00+01:00 [TOOL] Inspected `~/Downloads/115_GRCh38_vep.tar` as a potential replacement cache source. Top-level payload sizes inside the tar are approximately: `variation.fjall=116.6 GB`, `variation=39.7 GB`, `translation_sift.fjall=28.0 GB`, `translation_sift=2.6 GB`, `transcript=0.37 GB`, `exon=0.21 GB`, `translation_core=0.15 GB`, `regulatory=0.08 GB`, `motif<1 MB`. Local free space on `/System/Volumes/Data` is about `68 GiB`, and the installed `bsdtar` lacks an in-place delete/update workflow for shrinking the source archive during staged extraction. Conclusion: a full local tar-to-expanded-cache conversion is not feasible without extra storage or moving/removing the archive first; selective extraction of smaller subtrees is feasible.
- 2026-03-29T11:05:00+01:00 [TOOL] Extracted the non-`fjall` cache directories from `~/Downloads/115_GRCh38_vep.tar` into repo-local `.cache/vepyr_cache/parquet/115_GRCh38_vep` and switched local `.env` `VEPYR_DIFFLY_VEPYR_CACHE_OUTPUT_DIR` to `/Users/lukaszjezapkowicz/Desktop/magisterka/praca/vepyr_diffly/.cache/vepyr_cache`.
- 2026-03-29T13:30:00+01:00 [CODE] Changed annotator subprocess execution to stream stdout/stderr directly into `runtime/vep.log` and `runtime/vepyr.log` while the subprocess runs, instead of buffering all output and writing the log only after exit.
- 2026-03-29T13:30:00+01:00 [CODE] Stopped deleting `runtime/effective_config.json` during cleanup; the prior cleanup order removed that file immediately after writing it.
- 2026-03-29T14:20:00+01:00 [USER] Requested aggressive cleanup of `runs/`, keeping only `runs/full-golden-fresh` and removing stale `vepyr`/compare artifacts from that directory.
- 2026-03-29T14:15:00+01:00 [CODE] Added a faster compare path with configurable `compare_mode`, `bucket_count`, `compare_workers`, and `fingerprint_only` options. Large consequence compares now do an exact per-bucket precheck first and in `fast` mode run `diffly` only for buckets that actually differ.
- 2026-03-29T14:15:00+01:00 [CODE] Added per-stage timing capture to the compare pipeline, propagated compare settings into `RuntimeConfig` and `effective_config.json`, and extended JSON/Markdown summaries with timings plus consequence compare details.
- 2026-03-29T14:15:00+01:00 [CODE] Added adaptive larger bucket chunk sizes for large VCFs to reduce consequence bucket file fragmentation and I/O overhead.
- 2026-03-29T13:30:00+01:00 [TOOL] Verified the repo-local parquet cache with a smoke annotation plus `compare-existing`: `runs/local-smoke-new-cache-compare` reported variant tier `1000` joined equal and consequence tier `34741` joined equal with zero mismatches.
- 2026-03-29T13:30:00+01:00 [TOOL] Verified `PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_runtime.py tests/test_sampling.py` with `5 passed` after the log-streaming and cleanup-order fixes.
- 2026-03-29T14:20:00+01:00 [TOOL] Removed all run directories except `runs/full-golden-fresh`, and from that directory removed stale `vepyr.annotated.vcf`, prepared/sampled inputs, normalized outputs, summaries, and diff artifacts. After cleanup, `runs/full-golden-fresh/runtime/vep.annotated.vcf` remained but is only about `38M`, so the earlier full `16G` VEP artifact is no longer present under `runs/`.
- 2026-03-29T14:15:00+01:00 [TOOL] Verified `source .venv/bin/activate && PYTHONPATH=src pytest -q` with `17 passed` after the fast/debug compare refactor.
- 2026-03-29T14:15:00+01:00 [TOOL] Verified `source .venv/bin/activate && PYTHONPATH=src python -m vepyr_diffly.cli compare-existing --preset ensembl_everything --left-vcf tests/fixtures/annotated_left.vcf --right-vcf tests/fixtures/annotated_right.vcf --output-dir /tmp/vepyr-diffly-fast-smoke --compare-mode fast`; the run completed successfully and printed stage timings in the console summary.
- 2026-03-29T14:55:00+01:00 [TOOL] Verified the new dual-mode smoke matrix on sample sizes `10`, `100`, `1000`, and `10000` using `run --compare-mode fast` followed by `compare-existing --compare-mode debug` against the generated annotated VCFs. All runs reported `variant.equal=true` and `consequence.equal=true` in both modes; the `10000` sample expanded to `10066` prepared single-alt records. The matrix was executed under `/tmp` and the temporary directories were removed immediately after completion (`/tmp/vepyr-diffly-modes-*` absent afterward).
- 2026-03-29T15:10:00+01:00 [TOOL] Benchmarked compare-only behavior on a larger equal sample (`10000` requested variants, `10066` prepared records) under `/tmp`. `compare-existing --compare-mode fast` reported `variant.equal=true`, `consequence.equal=true`, `bucketization_s≈2.056`, `consequence_diff_s≈2.179`, `precheck_equal_buckets=8`, `exact_diff_run_buckets=0`. `compare-existing --compare-mode debug` on the same annotated VCFs reported `bucketization_s≈2.187`, `consequence_diff_s≈4.290`, `precheck_equal_buckets=8`, `exact_diff_run_buckets=8`. Temporary benchmark data was removed immediately afterward.
- 2026-03-29T15:30:00+01:00 [CODE] Further optimized consequence preprocessing and compare internals: replaced the Python `map_elements` allele-normalization hot path with a native Polars expression, reduced chunk parser overhead by building consequence chunk DataFrames from column lists with only the columns needed for consequence processing, and added a post-bucketization compaction step that merges `part-*.parquet` into a single `bucket.parquet` per bucket.
- 2026-03-29T15:30:00+01:00 [CODE] Updated bucket compare discovery/use so `compare-existing` consumes compacted `bucket.parquet` files directly when available, avoiding repeated per-bucket regrouping over many chunk parts during precheck and exact diff stages.
- 2026-03-29T15:40:00+01:00 [CODE] Removed another full-file preprocessing pass by threading variant-summary row counts into consequence bucketization instead of calling `_count_vcf_records(...)` again. Also reordered consequence chunk normalization so `_csq_allele` is extracted and filtered against the normalized ALT before materializing the full CSQ field payload, reducing wasted work on rows that are discarded anyway.
- 2026-03-29T15:55:00+01:00 [CODE] Replaced the `fast`-mode bucket precheck joins with a single eager exact comparison of sorted compacted bucket frames. Equal buckets now pay for one scan+sort per side instead of multiple lazy joins/count passes, while mismatching buckets still fall back to exact `diffly`.
- 2026-03-29T16:05:00+01:00 [CODE] Moved the chunked consequence parser to emit `csq_entries: list[str]` directly from the raw INFO bytes instead of emitting a large `CSQ` string and splitting by comma later inside Polars. This preserves semantics but shifts the comma split earlier into the byte parser.
- 2026-03-29T16:20:00+01:00 [CODE] Added operational tooling around long-running compare work: `cli annotate-vepyr` to run only the `vepyr` annotator on an existing prepared input using the configured cache/interpreter, `cli benchmark-compare` to benchmark `compare-existing` on an existing annotated VCF pair in temporary directories, and richer command logs in `runtime.py` that now include `STARTED`, `ENDED`, and `EXIT` markers.
- 2026-03-29T16:20:00+01:00 [CODE] Hardened `vepyr_runner.py` so it prepends the repo-local `vepyr/src` path automatically and raises a clearer runtime error when a non-working `vepyr` import is used instead of a compiled `.vepyr` interpreter.
- 2026-03-30T07:41:04Z [TOOL] Verified the RAM-safe refactor with `python -m py_compile`, `ruff format .`, `ruff check .`, targeted regression tests, and full `PYTHONPATH=src pytest -q`; all checks passed (`20 passed`).
- 2026-03-30T07:41:04Z [ASSUMPTION] The new resource planner uses conservative heuristics from file size and memory budget rather than measuring live RSS; bounded-memory safety now depends on chunk/bucket/worker limits plus runtime/container enforcement, not dynamic host-memory probing.
- 2026-03-30T10:22:00+02:00 [TOOL] The `.vepyr` interpreter configured in `.env` can import `vepyr` successfully from its own `site-packages`; the immediate runtime failure came from `vepyr_runner.py` prepending `repo/vepyr/src`, which forced import of an unbuilt checkout lacking `vepyr._core`.
- 2026-03-30T10:22:00+02:00 [TOOL] After the import-order fix, local end-to-end `run --sample-first-n 10 --compare-mode fast --memory-budget-mb 512` completed successfully with `variant.equal=true` and `consequence.equal=true`; additional smoke points `100/1000/10000` also completed successfully in the same mode matrix, while the `100000` end-to-end smoke was intentionally stopped after confirming it had progressed through VEP and into `vepyr` annotation because it was taking materially longer than the smaller smoke points.
- 2026-03-30T11:15:00+02:00 [TOOL] The retained full compare-only golden artifact `runs/full-golden-compare-fast` finished in about `49m` under `memory_budget_mb=1024`; variant tier was fully equal, while consequence tier reported `392` left-only and `392` right-only rows with `0` unequal rows, which currently points more toward transcript-set drift than payload disagreement on matched keys.

[OUTCOMES]
- 2026-03-29T14:55:00+01:00 [TOOL] Completed requested smoke verification for both compare modes over `10/100/1000/10000` sampled variants. Outcome: VEP and `vepyr` remained fully equal on both tiers in every tested size for both `fast` and `debug` modes, and smoke artifacts were cleaned up instead of being retained under `runs/`.
- 2026-03-29T15:10:00+01:00 [TOOL] Larger compare benchmark confirms the new `fast` mode is materially faster than `debug` on an equal dataset because it skips exact `diffly` bucket runs when prechecks prove equality. However, bucketization time remains essentially unchanged, so the refactor reduces the full compare cost but does not eliminate the dominant preprocessing bottleneck.
- 2026-03-29T15:30:00+01:00 [TOOL] Verified the new compacted-bucket path with `python -m py_compile`, `PYTHONPATH=src pytest -q tests/test_compare.py tests/integration/test_cli_compare_existing.py` (`4 passed`), and a `/tmp` equal-file `compare-existing --compare-mode fast` sanity check that returned `variant.equal=true` and `consequence.equal=true`. Temporary smoke directories were removed immediately afterward.
- 2026-03-29T15:40:00+01:00 [TOOL] Verified the follow-up preprocessing changes with `python -m py_compile`, `PYTHONPATH=src pytest -q tests/test_compare.py tests/integration/test_cli_compare_existing.py tests/test_runtime.py` (`6 passed`), and a `/tmp` equal-file `compare-existing --compare-mode fast` sanity check that returned `variant.equal=true` and `consequence.equal=true`. Temporary smoke directories were removed immediately afterward.
- 2026-03-29T15:55:00+01:00 [TOOL] Re-benchmarked the equal `10000` compare-only case after the eager sorted-bucket precheck refactor. Result: `prepared_records=10066`, `fast bucketization_s≈2.534`, `fast consequence_diff_s≈0.855`, `debug bucketization_s≈2.549`, `debug consequence_diff_s≈2.195`, with both modes still reporting full equality. Compared to the earlier `10000` benchmark (`fast consequence_diff_s≈2.179`, `debug consequence_diff_s≈4.290`), exact compare time dropped materially while bucketization remained roughly flat/slightly worse. Temporary benchmark data was removed immediately afterward.
- 2026-03-29T16:05:00+01:00 [TOOL] Tested the `csq_entries` parser refactor with `python -m py_compile`, `PYTHONPATH=src pytest -q tests/test_compare.py tests/integration/test_cli_compare_existing.py tests/test_runtime.py` (`6 passed`), and another `/tmp` `10000` equal benchmark. Result: `fast bucketization_s≈2.722`, `fast consequence_diff_s≈0.892`, still fully equal. Conclusion: this particular parser change did not improve the measured `10000` bucketization time and is at best neutral/slightly worse at that scale.
- 2026-03-29T16:20:00+01:00 [TOOL] Verified the new operational commands with `python -m py_compile`, `PYTHONPATH=src pytest -q tests/test_compare.py tests/integration/test_cli_compare_existing.py tests/test_runtime.py` (`6 passed`), and `PYTHONPATH=src python -m vepyr_diffly.cli benchmark-compare --left-vcf tests/fixtures/annotated_left.vcf --right-vcf tests/fixtures/annotated_left.vcf --output-json /tmp/vepyr-diffly-benchmark.json`, which produced a valid JSON benchmark summary and cleaned the temp output file afterward.
- 2026-03-30T07:41:04Z [TOOL] The compare pipeline now exposes a concrete resource contract (`memory_budget_mb`, bucket count, chunk sizes, worker count, side parallelism) in `summary.json` and uses that plan to choose bounded-memory normalization and comparison paths for large files, including a new bucketized variant tier.
- 2026-03-30T10:22:00+02:00 [TOOL] Current local runtime state: `vepyr` import is repaired and small/medium end-to-end smoke runs are back to green; the remaining operator bottleneck for very large smoke points is annotation wall-clock time, not the compare pipeline or RAM safety.
- 2026-03-30T11:15:00+02:00 [TOOL] Documentation is now aligned with the current codepath and retained artifacts: local quickstart, compare-only guidance, RAM-budgeted execution, and the current full golden fast result are all described in one coherent README flow.
